export enum ScreenNames {
  Home = 'Home',
  Chat = 'Chat',
  Settings = 'Settings',
}
export enum sheetnames {
  languages = 'languages',
}
export type LabelValue = {
  label: string;
  value: string;
};
